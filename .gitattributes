# Normalize line endings: store LF in the repo, check out native on each platform.
* text=auto eol=lf
# Shell scripts must always be LF (they run inside Linux containers).
*.sh text eol=lf
*.png binary
*.jpg binary
*.ico binary
*.webp binary
